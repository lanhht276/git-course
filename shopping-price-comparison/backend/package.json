{
  "name": "shopping-price-comparison-backend",
  "version": "1.0.0",
  "description": "Backend API for shopping price comparison app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "mock": "node server.js",
    "test": "PORT=3001 TEST_PORT=3001 node ./server.js & sleep 1 && node ./test.js; kill $! 2>/dev/null",
    "live": "USE_MOCK=false node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "axios": "^1.6.0",
    "cheerio": "^1.0.0-rc.12",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
